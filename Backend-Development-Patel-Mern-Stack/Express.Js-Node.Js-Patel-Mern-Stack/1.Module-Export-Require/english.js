const english = require("./greeting.json");

console.log(english);
console.log(english.English);


// When you use require() to import a JSON file in JavaScript, Node.js automatically parses the JSON content and converts it into a JavaScript object.